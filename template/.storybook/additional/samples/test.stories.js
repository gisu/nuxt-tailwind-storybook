import { storiesOf } from '@storybook/vue'

storiesOf('Basic|Samples', module)
  .add('Add Url to Monitor', () => ({
    template: `
      <div class="p-50px">
        <h1 class="text-4xl font-bold">Add Url to Monitor</h1>
        
      </div>
    `
  }))
